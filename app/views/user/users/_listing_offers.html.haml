%table.table
  %thead
    %tr
      %th{scope: "col"} User
      %th{scope: "col"} NFT Post ID
      %th{scope: "col"} NFT Post Title
      %th{scope: "col"} Offer Amount
      %th{scope: "col"} Actions
  %tbody
    - @listing_offers.each do |offer|
      %tr
        %th{scope: "row"}
          = offer.user.name.presence || "User #{offer.user.id}"
        %td
          = offer.nft_post.id
        %td
          = offer.nft_post.title
        %td
          = offer.amount
        %td
          - if can_claim_offer?(offer.nft_post, offer.id)
            = link_to 'Claim Offer', 'javascript:void(0)', class: 'btn btn-primary', id: 'claimOffer'
